syntax = "proto3";

message CASE_msg
{
    float vehm_fl_slip = 1;
}
message config {
    float CASE_TORQUE_MAX = 1;
    float FRONT_BRAKE_BIAS = 2;
    bool TCS_ACTIVE = 3;
}

message HT_ETH_Union {
  oneof type_union {
    CASE_msg case_msg_ = 1;
    config config_ = 2;
  }
}